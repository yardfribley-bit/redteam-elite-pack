# 安全策略（Security & Responsible Use）

## ⚠️ 授权使用声明

RedTeam Elite Pack 是一个**防御导向的安全评估工具**，用于：

- 经授权的红队评估与渗透测试
- 自有模型 / 自托管端点的安全基线测量
- 学术界的大模型安全研究
- 安全产品（护栏、过滤器）的有效性验证

**严禁**将本项目或其产出用于：

- 未经明确授权的系统、模型或服务
- 生成并传播可造成实际危害的恶意代码
- 任何违反当地法律法规的行为

使用者需自行确保测试行为在授权范围内，并承担相应法律责任。项目维护者不对误用或滥用导致的任何后果负责。

---

## 🔐 报告本项目自身的安全问题

若你在本项目中发现安全缺陷（如路径遍历、命令注入、凭据泄露等），**请勿公开开 Issue**，请通过 GitHub 的 [Private Vulnerability Reporting](https://github.com/yardfribley-bit/redteam-elite-pack/security/advisories) 私下提交。

期望在你提交的内容中包含：

1. 问题描述与影响范围
2. 复现步骤（含使用的命令与参数）
3. 建议的修复方案（可选）

我们会在 **7 个工作日内**响应，并在修复后与你确认披露时间。

---

## 🛡️ 关于本项目内含的攻击性内容

本仓库包含越狱 prompt、恶意代码片段与系统提示泄露证据。这些内容之所以保留，是因为：

- 它们是**已实测的漏洞证据**，删掉会让结论失去可复现性
- 同类公开项目（garak、PyRIT 等）同样公开其探针
- 可复现性是大模型安全研究的基础

我们采取的做法是：**保留证据，但不提供一键武器化投递**。仓库不含任何 C2、投递、免杀或规模化利用组件。

---

## 🔑 凭据处理

- 引擎不存储、不回传任何 API key；key 仅在运行时通过 `--key` 或 `--key-file` 读取
- 默认 key 文件名 `or_key.txt` 已被 `.gitignore` 排除
- **若你 fork 本仓库，请确保你的 key 文件不会被提交**
